#!/bin/sh -e
# flatpak run --command=bash com.nvidia.geforcenow -c '--ozone-platform-hint=auto --enable-features=VaapiVideoDecodeLinuxGL'

flatpak run --socket=wayland com.nvidia.geforcenow --ozone-platform-hint=auto --ozone-platform=wayland --enable-features=UseOzonePlatform,VaapiVideoDecoder,VaapiVideoDecodeLinuxGL --enable-accelerated=video-decode